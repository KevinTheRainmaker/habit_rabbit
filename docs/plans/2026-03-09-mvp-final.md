# Habit Rabbit MVP Final — Implementation Plan

**Goal:** 통계 화면 프리미엄 티저 연결 + 빈 상태 개선 + 월별 달성률 강화

---

## Task 108: StatisticsScreen에 PremiumTeaserBanner 연결 (TDD) — Story 19

**Files:**

- Modify: `lib/presentation/screens/statistics_screen.dart`
- Modify: `test/widget/statistics_screen_test.dart`

**RED**: 테스트:

```dart
testWidgets('무료 사용자 통계 화면에 PremiumTeaserBanner 표시', (tester) async { ... });
testWidgets('프리미엄 사용자 통계 화면에 PremiumTeaserBanner 미표시', (tester) async { ... });
```

**GREEN**: StatisticsScreen이 User.isPremium을 받아 PremiumTeaserBanner 조건부 표시.

**Step 5: 커밋**

```bash
git commit -m "feat: show PremiumTeaserBanner in StatisticsScreen"
```

---

## Task 109: HabitListScreen 빈 상태 온보딩 CTA 개선 (TDD)

**Files:**

- Create: `lib/presentation/widgets/empty_habit_state.dart`
- Create: `test/widget/empty_habit_state_test.dart`

**RED**: 테스트:

```dart
testWidgets('첫 습관 추가 안내 텍스트 표시', (tester) async { ... });
testWidgets('"첫 습관 추가하기" 버튼 표시', (tester) async { ... });
testWidgets('"첫 습관 추가하기" 탭 시 onAdd 호출', (tester) async { ... });
```

**GREEN**: EmptyHabitState(onAdd) 위젯 — 안내 텍스트 + CTA 버튼.

**Step 5: 커밋**

```bash
git commit -m "feat: add EmptyHabitState widget with onboarding CTA"
```

---

## Task 110: WeeklyCompletionRateUseCase (TDD)

**Files:**

- Create: `lib/domain/usecases/weekly_completion_rate_usecase.dart`
- Create: `test/unit/domain/weekly_completion_rate_usecase_test.dart`

**RED**: 테스트:

```dart
test('7일 모두 체크인 시 100%', () { ... });
test('7일 중 4일 체크인 시 57%', () { ... });
test('체크인 없으면 0%', () { ... });
```

**GREEN**: WeeklyCompletionRateUseCase(checkins, today) → double.

**Step 5: 커밋**

```bash
git commit -m "feat: add WeeklyCompletionRateUseCase"
```

---

## Task 111: 전체 테스트 통과 확인

```bash
flutter test test/unit/ test/widget/
git commit -m "chore: mvp final complete"
```
